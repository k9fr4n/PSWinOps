name: CI - Lint and Test

on:
  pull_request:
    branches:
      - main
      - develop
  push:
    branches:
      - main
      - develop

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  # ─────────────────────────────────────────
  # JOB 1 — VALIDATE MODULE MANIFEST
  # ─────────────────────────────────────────
  validate:
    runs-on: windows-latest
    steps:
      - name: Configure git line endings
        run: git config --global core.autocrlf true

      - uses: actions/checkout@v4.2.2

      - name: Validate Module Manifest
        shell: pwsh
        run: |
          $result = Test-ModuleManifest -Path .\PSWinOps.psd1 -ErrorAction Stop
          Write-Output "[OK] Module manifest is valid"
          Write-Output "Module : $($result.Name)"
          Write-Output "Version: $($result.Version)"
          Write-Output "Author : $($result.Author)"

  # ─────────────────────────────────────────
  # JOB 2 — LINT
  # ─────────────────────────────────────────
  lint:
    runs-on: windows-latest
    steps:
      - name: Configure git line endings
        run: git config --global core.autocrlf true

      - uses: actions/checkout@v4.2.2

      - name: Cache PowerShell modules
        uses: actions/cache@v4.2.3
        with:
          path: C:\Users\runneradmin\Documents\PowerShell\Modules
          key: psmodules-psscriptanalyzer-${{ runner.os }}

      - name: Lint with PSScriptAnalyzer
        shell: pwsh
        run: |
          if (-not (Get-Module -ListAvailable -Name PSScriptAnalyzer)) {
            Install-Module -Name PSScriptAnalyzer -Force -SkipPublisherCheck
          }

          $files = Get-ChildItem -Path . -Recurse -Include *.ps1, *.psm1 |
            Where-Object { $_.FullName -notmatch '[\\/](Tests|\.git|output)[\\/]' }

          Write-Output "[INFO] Analyzing $($files.Count) file(s)..."

          $results = $files | ForEach-Object {
            Invoke-ScriptAnalyzer -Path $_.FullName -Severity Error, Warning
          }

          if ($results) {
            $results | Format-Table -AutoSize
            $errorCount   = ($results | Where-Object { $_.Severity -eq 'Error'   }).Count
            $warningCount = ($results | Where-Object { $_.Severity -eq 'Warning' }).Count
            throw "[FAIL] PSScriptAnalyzer found $errorCount error(s) and $warningCount warning(s)"
          }

          Write-Output "[OK] Linting passed - zero errors and warnings"

  # ─────────────────────────────────────────
  # JOB 3 — TESTS (parallel matrix by category)
  # ─────────────────────────────────────────
  test:
    needs: [validate, lint]
    runs-on: windows-latest
    strategy:
      fail-fast: true
      matrix:
        suite:
          - "Private"
          - "Public/activedirectory"
          - "Public/healthcheck"
          - "Public/network"
          - "Public/ntp"
          - "Public/proxy"
          - "Public/rdp"
          - "Public/system"
          - "Public/utils"

    steps:
      - name: Configure git line endings
        run: git config --global core.autocrlf true

      - uses: actions/checkout@v4.2.2

      - name: Cache PowerShell modules
        uses: actions/cache@v4.2.3
        with:
          path: C:\Users\runneradmin\Documents\PowerShell\Modules
          key: psmodules-pester5-${{ runner.os }}

      - name: Install Pester
        shell: pwsh
        run: |
          if (-not (Get-Module -ListAvailable -Name Pester | Where-Object { $_.Version -ge '5.7' })) {
            Install-Module -Name Pester -MinimumVersion 5.7 -Force -SkipPublisherCheck
          }

      - name: Run tests - ${{ matrix.suite }}
        shell: pwsh
        run: |
          $suiteName = "${{ matrix.suite }}"
          $suitePath = ".\Tests\$($suiteName -replace '/', '\')"
          $safeName  = $suiteName -replace '[/\\]', '-'

          $config = New-PesterConfiguration
          $config.Run.Path                = $suitePath
          $config.Output.Verbosity        = 'Normal'
          $config.TestResult.Enabled      = $true
          $config.TestResult.OutputPath   = "TestResults-$safeName.xml"
          $config.TestResult.OutputFormat = 'NUnitXml'

          $result = Invoke-Pester -Configuration $config

          Write-Output "[INFO] [$suiteName] $($result.PassedCount) passed, $($result.FailedCount) failed"

          if ($result.FailedCount -gt 0) {
            throw "[FAIL] $($result.FailedCount) test(s) failed in suite '$suiteName'"
          }

          Write-Output "[OK] Suite '$suiteName' passed"

      - name: Upload test results - ${{ matrix.suite }}
        uses: actions/upload-artifact@v4.6.2
        if: always()
        with:
          name: test-results-${{ strategy.job-index }}
          path: TestResults-*.xml
          retention-days: 30

  # ─────────────────────────────────────────
  # JOB 4 — PUBLISH COMBINED RESULTS
  # ─────────────────────────────────────────
  publish-results:
    needs: test
    runs-on: windows-latest
    if: always()
    permissions:
      checks: write
      pull-requests: write

    steps:
      - name: Download all test artifacts
        uses: actions/download-artifact@v4.2.1
        with:
          pattern: test-results-*
          merge-multiple: true
          path: all-results

      - name: Publish combined test results
        uses: EnricoMi/publish-unit-test-result-action/windows@v2.17.1
        with:
          files: all-results/**/*.xml
          check_name: Pester Test Results

  # ─────────────────────────────────────────
  # JOB 5 — CODE COVERAGE
  # ─────────────────────────────────────────
  coverage:
    needs: test
    runs-on: windows-latest
    if: always() # Run even if some tests failed — you still want the coverage comment
    permissions:
      contents: read
      pull-requests: write # Required for Codecov to post PR comments
      checks: write # Required for Codecov status checks

    steps:
      - name: Configure git line endings
        run: git config --global core.autocrlf true

      - uses: actions/checkout@v4.2.2

      - name: Cache PowerShell modules
        uses: actions/cache@v4.2.3
        with:
          path: C:\Users\runneradmin\Documents\PowerShell\Modules
          key: psmodules-pester5-${{ runner.os }}

      - name: Install Pester
        shell: pwsh
        run: |
          if (-not (Get-Module -ListAvailable -Name Pester | Where-Object { $_.Version -ge '5.7' })) {
            Install-Module -Name Pester -MinimumVersion 5.7 -Force -SkipPublisherCheck
          }

      - name: Run Pester with code coverage
        shell: pwsh
        run: |
          $config = New-PesterConfiguration
          $config.Run.Path                  = '.\Tests'
          $config.Output.Verbosity          = 'None'
          $config.CodeCoverage.Enabled      = $true
          $config.CodeCoverage.Path         = @(
              '.\PSWinOps.psm1',
              '.\Public\**\*.ps1',
              '.\Private\*.ps1'
          )
          $config.CodeCoverage.OutputPath   = 'coverage.xml'
          $config.CodeCoverage.OutputFormat = 'JaCoCo'

          Invoke-Pester -Configuration $config

      - name: Enforce coverage threshold
        shell: pwsh
        run: |
          if (-not (Test-Path 'coverage.xml')) {
            Write-Warning 'coverage.xml not found, skipping threshold check'
            exit 0
          }
          [xml]$coverage = Get-Content 'coverage.xml'
          $lineCounter = $coverage.report.counter | Where-Object { $_.type -eq 'LINE' }
          $covered = [int]$lineCounter.covered
          $missed  = [int]$lineCounter.missed
          $total   = $covered + $missed
          if ($total -gt 0) {
            $pct = [math]::Round(($covered / $total) * 100, 2)
            Write-Output "Coverage: $pct% ($covered/$total lines)"
            if ($pct -lt 70) {
              Write-Error "Coverage $pct% is below the 70% threshold"
              exit 1
            }
          }

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v5.4.2
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: coverage.xml
          flags: pester
          fail_ci_if_error: false
          verbose: true
          # Tell Codecov this is a single-upload workflow
          # so it doesn't wait for multiple reports before commenting
          override_pr: ${{ github.event.pull_request.number }}

      - name: Upload coverage artifact
        uses: actions/upload-artifact@v4.6.2
        if: always()
        with:
          name: coverage-report
          path: coverage.xml
          retention-days: 30
